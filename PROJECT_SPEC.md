# QuorumForge 根密钥签名仪式封存与隔离服务

## 项目目标

构建一个面向企业证书机构的单节点 Go 后端，围绕一次根密钥签名请求实现从摘要锁定、见证人到场确认、法定人数形成、HSM 会话登记、签名产物复核到封存或安全隔离的单一不可逆流程。仪式状态严格限定为待锁定、征集见证、待签名、待复核、已封存、安全隔离和已取消；锁定时冻结请求摘要、根密钥版本、策略版本及参与人范围，见证账簿保存不可撤销且按自然人去重的确认集合。角色不合格、摘要或密钥版本错配、重复见证、操作号内容冲突、签名令牌复用和陈旧修订均返回稳定错误且不得污染账簿。达到门限后只能建立一个 HSM 签名会话；封存要求法定人数、复核结论和唯一签名产物同时满足，封存与安全隔离的并发竞争只能产生一个终局。采用 SQLite WAL 事务仓库持久化仪式聚合、策略目录、参与人目录、见证/法定人数分录、HSM 会话、产物登记、幂等操作结果和审计事件；预设提交中断点确保失败事务不留下孤立对象，重启后恢复一致状态。实现目标为不少于 20 个生产 Go 文件、2000 行有效生产 Go、至少 4 个有意义的 Go 包，包含 go.mod、Go HTTP API、真实仪式操作页、多阶段 Dockerfile，并支持 linux/amd64 与 linux/arm64。公开测试拆分为至少 4 个测试文件和 12 个确定性用例，使用固定测试密钥、虚构参与人、临时数据库、同步屏障及可注入中断点覆盖并发与数据库恢复行为。

## 端到端业务流程

- 未定义

## 核心组件与职责

1. 策略与参与人目录包：维护策略版本、角色资格、门限、允许的密钥版本和参与人身份修订，并提供锁定时的不可变快照。
2. 签名仪式聚合包：实现七态状态机、请求摘要/密钥版本绑定、修订号、操作号幂等及终局栅栏。
3. 见证及法定人数账簿包：登记到场确认、按自然人唯一约束去重，拒绝重复或陈旧确认，并计算稳定排序的法定人数快照。
4. HSM 会话与产物登记包：验证一次性签名令牌，创建唯一 HSM 会话，记录会话回执、复核摘要和至多一份签名产物，支持封存或安全隔离。
5. 事务仓库与恢复包：基于 SQLite WAL 的事务边界、唯一索引、失败注入、提交后发布及重启恢复，保证仪式、见证、会话和产物不会出现孤立记录。
6. Go HTTP API 与仪式操作页：提供锁定、见证确认、开始签名、登记 HSM 回执、复核、封存、安全隔离、取消及查询接口；页面仅承载真实仪式操作并显示稳定错误和当前修订。

## 领域规则与不变量

- 未定义

## 数据模型与持久化

- 未定义

## 公开接口

- 未定义

## 失败边界

- 未定义

## 验收标准

1. 锁定操作必须固定请求摘要、根密钥版本、策略版本及参与人范围；任何后续请求只能引用该快照。
2. 同一自然人的多个凭据或并发确认最多计入一次法定人数，角色不合格或身份修订不匹配的确认不得写入账簿。
3. 达到门限后，无论并发重试次数，系统最多建立一个签名会话；会话必须绑定当前仪式修订、摘要和密钥版本。
4. 相同操作号携带相同规范化内容时返回原始成功结果并保持幂等；相同操作号内容冲突时稳定报错且不改变业务状态。
5. 陈旧修订、摘要错配、密钥版本错配或已撤销身份的命令不得改变见证账簿、会话记录或仪式修订。
6. 法定人数和复核结论未同时满足时不得封存签名产物；签名令牌只能成功消费一次，重复使用必须被拒绝。
7. 封存与安全隔离并发执行时只能形成一个终局状态，并且数据库中至多存在一份签名产物登记。
8. 任一预设事务中断点失败后不得留下孤立见证、会话或产物；重启并重新打开数据库后，已提交状态、幂等结果和终局栅栏保持一致。

## 确定性测试场景

- 未定义

## 组件追踪关系

- 未定义

## 独特性

项目把企业根密钥操作抽象为可审计的签名仪式聚合，核心难点是不可撤销的见证集合、门限形成与唯一 HSM 会话之间的严格绑定，并将复核、封存和安全隔离收敛到单一终局。它同时提供可操作的仪式页面、一次性令牌语义、身份与密钥版本栅栏以及可注入事务恢复边界，形成区别于普通审批或文件流转的密码基础设施工作流。
